export const UI_CSS = `
:root{color-scheme:light;--navy:#13294b;--blue:#4b9cd3;--ink:#172033;--muted:#5d6778;--line:#dce2ea;--paper:#fff;--wash:#f4f7fa;--good:#176b43;--warn:#8a4b08;--danger:#a61b1b}*{box-sizing:border-box}body{margin:0;background:var(--wash);color:var(--ink);font:16px/1.5 system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}button,input,select{font:inherit}button,.button{min-height:44px;border:0;border-radius:.5rem;background:var(--navy);color:#fff;padding:.65rem 1rem;cursor:pointer;text-decoration:none}.secondary{background:#e8f2f8;color:var(--navy)}.danger{background:var(--danger);color:#fff}button:focus-visible,input:focus-visible,select:focus-visible,a:focus-visible{outline:3px solid #f9d65c;outline-offset:2px}.skip{position:absolute;left:-999px}.skip:focus{left:1rem;top:1rem;background:#fff;padding:.5rem;z-index:9}.topbar{background:var(--navy);color:#fff}.topbar>div{max-width:76rem;margin:auto;padding:1rem 1.25rem;display:flex;align-items:center;gap:1rem;justify-content:space-between}.brand{font-weight:800;font-size:1.2rem}.brand span{color:var(--blue)}main{max-width:76rem;margin:auto;padding:2rem 1.25rem}.hero,.panel,.card{background:var(--paper);border:1px solid var(--line);border-radius:.8rem;box-shadow:0 5px 18px #13294b0b}.hero{padding:clamp(1.3rem,4vw,3rem);margin-bottom:1.25rem}.hero h1{margin:.1rem 0 .5rem;font-size:clamp(2rem,5vw,3.5rem);line-height:1.05}.hero p{max-width:45rem;color:var(--muted);font-size:1.08rem}.panel{padding:1.2rem;margin-bottom:1.25rem}.toolbar{display:grid;grid-template-columns:minmax(12rem,1fr) auto;gap:.75rem}.toolbar input,.toolbar select,form input{width:100%;min-height:44px;border:1px solid #aeb8c6;border-radius:.5rem;padding:.55rem .7rem}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(min(100%,19rem),1fr));gap:1rem}.card{padding:1.2rem}.card h2{margin:.1rem 0}.meta{color:var(--muted);font-size:.92rem}.status{display:inline-block;border-radius:99px;padding:.2rem .55rem;background:#e3f4eb;color:var(--good);font-weight:700;font-size:.8rem}.status.stale{background:#fff0df;color:var(--warn)}.deadline{font-size:1.2rem;font-weight:750}.empty{padding:2.5rem;text-align:center;color:var(--muted)}dialog{width:min(42rem,calc(100% - 2rem));border:0;border-radius:.8rem;padding:0;box-shadow:0 20px 70px #0005}dialog::backdrop{background:#13294b99}.dialog-body{padding:1.5rem}.dialog-head{display:flex;justify-content:space-between;align-items:center;gap:1rem}.dialog-head button{background:transparent;color:var(--ink);font-size:1.5rem;padding:.2rem;min-height:40px}.stack{display:grid;gap:.8rem}.notice{border-left:4px solid var(--blue);background:#eaf4fa;padding:.8rem}.error{border-left-color:var(--danger);background:#ffeded}.hidden{display:none!important}.evidence{border-top:1px solid var(--line);padding-top:.8rem;margin-top:.8rem}.evidence pre{white-space:pre-wrap;overflow-wrap:anywhere;background:var(--wash);padding:.7rem;border-radius:.4rem;font-size:.85rem}.admin-actions,.connector-row,.privacy-actions{display:flex;flex-wrap:wrap;gap:.5rem;margin-top:.8rem}.connector-row{align-items:center;justify-content:space-between;border-top:1px solid var(--line);padding-top:.8rem}.connector-row p{margin:0}.consent{display:flex;align-items:flex-start;gap:.6rem}.consent input{margin-top:.35rem}.privacy-actions{border-top:1px solid var(--line);padding-top:1rem}footer{max-width:76rem;margin:auto;padding:1rem 1.25rem 3rem;color:var(--muted);font-size:.9rem}@media(max-width:38rem){.toolbar{grid-template-columns:1fr}.topbar>div{align-items:flex-start}.hero{padding:1.3rem}}
`;

export const UI_JS = `
(function(){'use strict';
var state={user:null,organizations:[],providers:['gmail','groupme','instagram','linkedin']};
var byId=function(id){return document.getElementById(id)};
var csrf=function(){var item=document.cookie.split('; ').find(function(value){return value.indexOf('recruiting_csrf=')===0});return item?decodeURIComponent(item.split('=').slice(1).join('=')):''};
var request=async function(path,options){options=options||{};options.headers=Object.assign({'content-type':'application/json'},options.headers||{});if(options.method&&options.method!=='GET')options.headers['x-csrf-token']=csrf();var response=await fetch(path,options);var data=await response.json().catch(function(){return{}});if(!response.ok)throw new Error(data.error&&data.error.message||'Request failed');return data};
var escapeHtml=function(value){return String(value==null?'':value).replace(/[&<>"']/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]})};
var dateText=function(value,precision){if(!value)return'Date being confirmed';var d=new Date(value);if(Number.isNaN(d.getTime()))return'Date being confirmed';if(precision==='date')return new Intl.DateTimeFormat('en-US',{dateStyle:'long',timeZone:'America/New_York'}).format(d)+' · time not specified';return new Intl.DateTimeFormat('en-US',{dateStyle:'medium',timeStyle:'short',timeZone:'America/New_York'}).format(d)+' ET'};
var showMessage=function(text,isError){var el=byId('message');el.textContent=text;el.className='notice'+(isError?' error':'');el.classList.remove('hidden')};
var render=function(){var query=byId('search').value.toLowerCase();var sort=byId('sort').value;var orgs=state.organizations.filter(function(org){return(org.name+' '+org.school).toLowerCase().indexOf(query)>=0});orgs.sort(function(a,b){if(sort==='deadline'){var ad=(a.opportunities[0]||{}).deadlineAt||'9999';var bd=(b.opportunities[0]||{}).deadlineAt||'9999';return ad.localeCompare(bd)}return a.name.localeCompare(b.name)});var grid=byId('opportunities');if(!orgs.length){grid.innerHTML='<div class="panel empty">No matching organizations yet.</div>';return}grid.innerHTML=orgs.map(function(org){var items=org.opportunities.length?org.opportunities:[{title:'Recruiting details',explanation:'No current opportunity has been confirmed.',stale:true,confidence:0}];return items.map(function(op){var when=op.deadlineAt?'<p class="deadline">Deadline: '+escapeHtml(dateText(op.deadlineAt,op.deadlinePrecision))+'</p>':op.startsAt?'<p class="deadline">Starts: '+escapeHtml(dateText(op.startsAt,op.startsAtPrecision))+'</p>':'<p class="deadline">Timing being confirmed</p>';return'<article class="card"><span class="status '+(op.stale?'stale':'')+'">'+(op.stale?'Past or closed':'Current')+'</span><h2>'+escapeHtml(org.name)+'</h2><p class="meta">'+escapeHtml(op.title)+' · '+Math.round((op.confidence||0)*100)+'% confidence</p>'+when+'<p>'+escapeHtml(op.explanation)+'</p>'+(op.url?'<a class="button" rel="noopener noreferrer" target="_blank" href="'+escapeHtml(op.url)+'">Open application</a>':'')+(state.user&&state.user.roles.some(function(r){return r!=='student'})?'<div class="admin-actions"><button class="secondary evidence-button" data-org="'+escapeHtml(org.id)+'">Review evidence</button><button class="secondary source-button" data-org="'+escapeHtml(org.id)+'">Add source</button></div>':'')+'</article>'}).join('')}).join('');document.querySelectorAll('.evidence-button').forEach(function(button){button.addEventListener('click',function(){openEvidence(button.dataset.org)})});document.querySelectorAll('.source-button').forEach(function(button){button.addEventListener('click',function(){byId('source-org').value=button.dataset.org;byId('source-dialog').showModal()})})};
var load=async function(){try{var me=await request('/api/me');state.user=me.user;byId('user-name').textContent=me.user.email||me.user.displayName||'Signed in';byId('logout').classList.remove('hidden');byId('connectors').classList.remove('hidden');var dashboard=await request('/api/dashboard');state.organizations=dashboard.organizations;byId('signed-out').classList.add('hidden');byId('workspace').classList.remove('hidden');render();if(new URLSearchParams(location.search).get('connectors')==='1')openConnectors()}catch(error){byId('signed-out').classList.remove('hidden');byId('workspace').classList.add('hidden')}};
var openConnectors=async function(){if(!byId('connector-dialog').open)byId('connector-dialog').showModal();await Promise.all(state.providers.map(async function(provider){var row=document.querySelector('[data-connector="'+provider+'"]');var status=row.querySelector('.connector-status');var button=row.querySelector('.connector-action');try{var data=await request('/api/connectors/'+provider+'/status');status.textContent=data.connected?'Connected'+(data.expiresAt?' until '+dateText(data.expiresAt):''):'Not connected';button.textContent=data.connected?'Disconnect':'Connect';button.dataset.connected=String(data.connected)}catch(error){status.textContent='Unavailable';button.disabled=true}}))};
var openEvidence=async function(orgId){try{var data=await request('/api/admin/organizations/'+encodeURIComponent(orgId)+'/evidence');byId('evidence-content').innerHTML=data.claims.length?data.claims.map(function(claim){return'<section class="evidence"><strong>'+escapeHtml(claim.field)+'</strong><p class="meta">Confidence '+Math.round(claim.confidence*100)+'%</p><pre>'+escapeHtml(JSON.stringify(claim.value,null,2))+'</pre><p>'+escapeHtml(claim.evidence||'No excerpt recorded')+'</p></section>'}).join(''):'<p>No evidence has been processed yet.</p>';byId('evidence-dialog').showModal()}catch(error){showMessage(error.message,true)}};
byId('search').addEventListener('input',render);byId('sort').addEventListener('change',render);
byId('dev-login').addEventListener('submit',async function(event){event.preventDefault();try{var email=byId('email').value;var data=await request('/auth/development',{method:'POST',body:JSON.stringify({email:email,displayName:email.split('@')[0]})});document.cookie='recruiting_csrf='+encodeURIComponent(data.csrfToken)+'; path=/; SameSite=Strict';await load()}catch(error){showMessage(error.message,true)}});
byId('logout').addEventListener('click',async function(){try{await request('/auth/logout',{method:'POST',body:'{}'})}finally{location.reload()}});
byId('connectors').addEventListener('click',openConnectors);
byId('account-export').addEventListener('click',async function(){try{var data=await request('/api/me/export');var blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'});var link=document.createElement('a');link.href=URL.createObjectURL(blob);link.download='recruiting-os-account-export.json';link.click();URL.revokeObjectURL(link.href);showMessage('Your account export was downloaded.',false)}catch(error){showMessage(error.message,true)}});
byId('account-delete').addEventListener('click',async function(){if(!confirm('Delete your Recruiting OS account, connector credentials, sessions, and private evidence that nobody else contributed? This cannot be undone.'))return;try{await request('/api/me',{method:'DELETE',body:JSON.stringify({confirmation:'DELETE_MY_ACCOUNT'})});location.reload()}catch(error){showMessage(error.message,true)}});
document.querySelectorAll('.connector-action').forEach(function(button){button.addEventListener('click',async function(){var provider=button.closest('[data-connector]').dataset.connector;try{if(button.dataset.connected==='true'){await request('/api/connectors/'+provider,{method:'DELETE',body:'{}'});showMessage(provider+' disconnected and pending syncs cancelled.',false);await openConnectors()}else{if(!byId('connector-consent').checked)throw new Error('Confirm the privacy notice before connecting.');var data=await request('/api/connectors/'+provider+'/connect',{method:'POST',body:JSON.stringify({consentToProcess:true})});location.assign(data.authorizationUrl)}}catch(error){showMessage(error.message,true)}})});
byId('source-form').addEventListener('submit',async function(event){event.preventDefault();try{var body={organizationId:byId('source-org').value,url:byId('source-url').value};var data=await request('/api/ingest/url',{method:'POST',headers:{'x-idempotency-key':crypto.randomUUID()},body:JSON.stringify(body)});byId('source-dialog').close();showMessage('Source queued for review. Job '+data.jobId,false)}catch(error){showMessage(error.message,true)}});
document.querySelectorAll('[data-close]').forEach(function(button){button.addEventListener('click',function(){button.closest('dialog').close()})});load();
})();
`;

export function appPage(authMode: string): string {
  const login =
    authMode === 'oidc'
      ? '<a class="button" href="/auth/login">Sign in with UNC</a>'
      : '<form id="dev-login" class="stack"><label for="email">UNC email</label><input id="email" type="email" required value="student@unc.edu"><button type="submit">Development sign in</button></form>';
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="Current, evidence-backed UNC student organization recruiting deadlines"><title>Recruiting OS · UNC</title><link rel="stylesheet" href="/assets/app.css"></head><body><a class="skip" href="#main">Skip to content</a><header class="topbar"><div><div class="brand"><span>UNC</span> Recruiting OS</div><div><span id="user-name"></span> <button id="connectors" class="secondary hidden">Connectors & privacy</button> <button id="logout" class="secondary hidden">Sign out</button></div></div></header><main id="main"><section class="hero"><h1>Find the right club before the deadline.</h1><p>One accessible place for evidence-backed recruiting dates, events, and application links across UNC student organizations.</p></section><div id="message" role="status" aria-live="polite" class="notice hidden"></div><section id="signed-out" class="panel"><h2>Sign in to view recruiting opportunities</h2><p>Access is limited to approved UNC accounts.</p>${login}</section><section id="workspace" class="hidden" aria-labelledby="opportunity-heading"><div class="panel toolbar"><label>Search organizations<input id="search" type="search" placeholder="Search by name"></label><label>Sort<select id="sort"><option value="name">Organization name</option><option value="deadline">Nearest deadline</option></select></label></div><h2 id="opportunity-heading">Recruiting opportunities</h2><div id="opportunities" class="grid" aria-live="polite"></div></section></main><dialog id="evidence-dialog"><div class="dialog-body"><div class="dialog-head"><h2>Evidence review</h2><button data-close aria-label="Close">×</button></div><div id="evidence-content"></div></div></dialog><dialog id="source-dialog"><form id="source-form" class="dialog-body stack"><div class="dialog-head"><h2>Add a recruiting source</h2><button type="button" data-close aria-label="Close">×</button></div><input id="source-org" type="hidden"><label for="source-url">Public web or application URL</label><input id="source-url" type="url" required placeholder="https://example.org/apply"><p class="meta">The URL will be fetched by a constrained background worker and reviewed as evidence.</p><button type="submit">Queue source</button></form></dialog><dialog id="connector-dialog"><div class="dialog-body stack"><div class="dialog-head"><h2>Connectors & privacy</h2><button type="button" data-close aria-label="Close">×</button></div><p>Connectors use recruiting-related content to identify dates and links. Gmail access is read-only. Private raw messages and screenshots are retained for up to 90 days; derived recruiting facts may remain while current.</p><p>You can disconnect below at any time. This revokes the stored credential and cancels pending syncs; also revoke the app at the provider if you want to invalidate its token immediately.</p><label class="consent"><input id="connector-consent" type="checkbox"> I consent to Recruiting OS processing recruiting-related content under this notice.</label>${['gmail', 'groupme', 'instagram', 'linkedin'].map((provider) => `<div class="connector-row" data-connector="${provider}"><p><strong>${provider[0]?.toUpperCase()}${provider.slice(1)}</strong><br><span class="connector-status meta">Checking…</span></p><button class="connector-action" type="button">Connect</button></div>`).join('')}<section><h3>Your account data</h3><p class="meta">Download a metadata-only account export. Access tokens and raw private evidence are never included.</p><div class="privacy-actions"><button id="account-export" class="secondary" type="button">Download my data</button><button id="account-delete" class="danger" type="button">Delete my account</button></div></section></div></dialog><footer>Recruiting information is evidence-backed but may change. Confirm critical dates with the organization. Private source processing requires consent and follows the 90-day raw-content retention policy.</footer><script src="/assets/app.js" defer></script></body></html>`;
}
